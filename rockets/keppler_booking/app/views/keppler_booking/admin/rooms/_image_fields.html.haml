.nested-fields.col-lg-12
	- unless f.object.img.to_s == ""
		= image_tag f.object.img.url, style: "width: 30%"

	.row
		.col-lg-6
			= f.input :img, as: :keppler_file, label: false, wrapper_html: { class: "file-field" }
		
		.col-lg-6
			= link_to_remove_association f do
				%i.icon-trash
